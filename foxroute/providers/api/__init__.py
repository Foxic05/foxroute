"""Официальные API — по ключу, с документированным протоколом.

Отличаются от веб-сессий (``foxroute.providers.web``) по всем существенным
признакам, поэтому и лежат отдельно:

* протокол опубликован и стабилен, ломается не внезапно, а с
  предупреждением и версионированием;
* лимиты приходят заголовками ``x-ratelimit-*`` — то есть известны ДО того,
  как упрёшься;
* считаются токены, а не сообщения, и параллельные запросы допустимы
  (``slots`` больше единицы);
* но за них платят: либо деньгами, либо узким бесплатным тиром.

В нашем пуле их немного и роль у них вспомогательная. Основой их не
сделать: даже самый быстрый API-ключ однажды кончится, а веб-сессии живут
на бесплатных аккаунтах и масштабируются их количеством.
"""
